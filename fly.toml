app = "mini-mixlr-[your-name]"   # ← CHANGE [your-name] to anything (e.g. mini-mixlr-john)

[build]
  dockerfile = "Dockerfile"

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = true
  auto_start_machines = true
  min_machines_running = 0